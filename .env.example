# Copy to .env for `docker compose up`. This file is committed; .env is NOT.
#
# AI Forecast Assistant (optional). Get a key at https://aistudio.google.com/apikey
# Used server-side only — it is never sent to the browser and never baked into
# a Docker image. Leave blank to run without the assistant.
GEMINI_API_KEY=
