[pytest]
testpaths = tests
addopts = -q --tb=short -m "not slow"
markers =
    slow: long-running (live inference, subprocess portability). Run with -m slow
filterwarnings =
    ignore::DeprecationWarning
